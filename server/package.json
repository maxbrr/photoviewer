{
  "name": "photoviewer",
  "version": "1.0.0",
  "description": "Node.js / Vue.js application to consume jsonplaceholder-api",
  "main": "server.js",
  "scripts": {
    "dev": "set NODE_ENV=developement && nodemon server.js",
    "start": "set PORT=3000 && node server.js"
  },
  "author": "maxbrr",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "express": "^4.17.3"
  }
}
